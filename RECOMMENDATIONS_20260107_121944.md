# Recommendations

## 2026-01-07 12:19:44
- Consider adding resumable download support in the pipeline (range requests + state).
- Add integration tests that run against a small fixture corpus to validate OCR + NER end-to-end.
- Add a doctor check to validate MCP server endpoints are reachable when enabled.
- Evaluate adding a lightweight entity linking stage to reduce duplicates in relationship analysis.
